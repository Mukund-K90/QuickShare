<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Sign Up - Chat App</title>
    <link href="https://fonts.googleapis.com/css2?family=Poppins:wght@300;400;600&display=swap" rel="stylesheet">
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet" 
        integrity="sha384-QWTKZyjpPEjISv5WaRU9OFeRpok6YctnYmDr5pNlyT2bRjXh0JMhjY6hW+ALEwIH" crossorigin="anonymous">

    <style>
        * {
            box-sizing: border-box;
            margin: 0;
            padding: 0;
        }

        body {
            height: 100vh;
            display: flex;
            justify-content: center;
            align-items: center;
            background-image: url('../../assets/images/home_bg.jpg');
            background-size: cover;
            background-position: center;
            font-family: 'Poppins', sans-serif;
            overflow: hidden;
        }

        .container-wrapper {
            width: 90%;
            height: 90%;
            background-color: transparent;
            backdrop-filter: blur(10px);
            padding: 20px;
            border: 1px solid #98E2C6;
            border-radius: 8px;
            box-shadow: 0 4px 10px rgba(0, 0, 0, 0.1);
            display: flex;
            flex-direction: row;
        }

        .intro-img {
            max-height: 50%;
            width: 50%;
            object-fit: cover;
        }

        .intro-section {
            text-align: center;
        }

        .row {
            height: 100%;
            display: flex;
            justify-content: center;
            align-items: center;
            width: 100%;
        }

        .col-half {
            padding: 15px;
            width: 50%;
        }

        .form-control {
            width: 100%;
            padding: 10px;
            border-radius: 5px;
            border: 1px solid #ccc;
            margin-bottom: 15px;
        }

        .btn-primary {
            background-color: #337ab7;
            border-color: #2e6da4;
            padding: 10px 15px;
            width: 100%;
            border-radius: 5px;
            color: white;
            border: none;
        }

        .btn-primary:hover {
            background-color: #286090;
            border-color: #204d74;
        }

        .btn-block {
            width: 100%;
        }

        p {
            margin-top: 10px;
        }

        .mt-3 {
            margin-top: 1rem;
        }

        #flashMessage {
            position: fixed;
            top: 10%;
            left: 85%;
            transform: translateX(-50%);
            z-index: 9999;
            width: 45%;
            max-width: 300px;
            margin: 0 auto;
        }
    </style>
</head>

<body>
    <% if (messages.success) { %>
        <div id="flashMessage" class="alert alert-success">
            <%= messages.success %>
        </div>
    <% } %>
    <% if (messages.error) { %>
        <div id="flashMessage" class="alert alert-danger">
            <%= messages.error %>
        </div>
    <% } %>
    <div class="container-wrapper">
        <div class="row">
            <!-- Left Side: Image and Intro -->
            <div class="col-half intro-section">
                <img src="../../assets/images/pngwing.com (3).png" alt="Chat App" class="intro-img mb-2">
                <h3>Welcome to QuickShare</h3>
            </div>

            <!-- Right Side: Sign Up Form -->
            <div class="col-half">
                <h2>Create Account</h2>
                <form action="/v1/user/register" method="POST">
                    <input type="text" class="form-control" id="name" name="name"
                        placeholder="Full Name" value="<%= userData ? userData.name : '' %>" required>
                    <input type="email" class="form-control" id="email" name="email"
                        placeholder="Email address" value="<%= userData ? userData.email : '' %>" required>
                    <input type="mobile" class="form-control" id="mobile" name="mobile"
                        placeholder="Mobile" maxlength="10" value="<%= userData ? userData.mobile : '' %>" required>
                    <input type="password" class="form-control" id="password" name="password"
                        placeholder="Password" value="<%= userData ? userData.password : '' %>" required>
                    <input type="password" class="form-control" id="confirmPassword" name="confirmPassword"
                        placeholder="Confirm Password" value="<%= userData ? userData.confirmPassword : '' %>" required>
                    <button type="submit" class="btn-primary">Sign Up</button>
                </form>
                <p class="mt-3">Already have an account? <a href="/login">Login</a></p>
            </div>
        </div>
    </div>
    <script>
        setTimeout(() => {
            document.getElementById("flashMessage").style.display = 'none';
        }, 2000);
    </script>
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js" 
        integrity="sha384-YvpcrYf0tY3lHB60NNkmXc5s9fDVZLESaAA55NDzOxhy9GkcIdslK1eN7N6jIeHz" crossorigin="anonymous"></script>
</body>

</html>
